const set = false;

const setSettings = {
  developerMode: false
};

const log = (text) => {
  console.log(
    '%c[' + '%cDATK%c' + '] %c' + text, 'color: gray', 'color: #ff7f50', 'color: gray', 'clear'
  );
};